runtime: python312
# Specify the entrypoint for App Engine to map to our Flask app
entrypoint: gunicorn -b :$PORT main:app

handlers:
# Map the static directory directly so App Engine serves it efficiently without hitting the Python code
- url: /static
  static_dir: static

# Route everything else to the Python script
- url: /.*
  script: auto
